# Calibration

Calibration records are versioned evidence, not runtime configuration. A capability is publishable only when its fixture set is human-reviewed and the recorded measurements meet `gates.yaml`. Synthetic seed cases exercise the harness but cannot satisfy a publication gate.

Threshold changes require a new record with reviewer, date, rationale, model provenance, and before/after measurements.
