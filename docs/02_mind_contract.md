# Mind Contract

All Minds operate on the shared `MindInput` and `MindOutput` schemas defined in `tm_core/message_types.py`. The `BaseMind` class in `tm_core/base_mind.py` captures the expected lifecycle methods for loading, saving, thinking, training, and visualization.
