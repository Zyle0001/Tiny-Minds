[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny-minds"
version = "0.1.0"
description = "Prototype society of tiny interpretable neural modules"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24",
]
